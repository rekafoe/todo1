import './globals.css';

export const metadata = {
  title: 'To-Do',
};

export default function RootLayout({ }) {
  return (
    <html lang="ru">
      <body>
        {}
      </body>
    </html>
  );
}